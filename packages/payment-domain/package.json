{
  "name": "@payflow/payment-domain",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@payflow/database": "workspace:*",
    "@payflow/observability": "workspace:*",
    "@payflow/payment-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
