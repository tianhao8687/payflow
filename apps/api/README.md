# PayFlow API

NestJS application for the PayFlow modular monolith. Stage 1 contains system,
health, auth/users, and read-only products modules with global JWT/RBAC guards,
throttling, validation, Swagger, and a uniform error envelope. See the repository
root README for setup, commands, and the current stage boundary.
